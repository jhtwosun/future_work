---
arxiv_id: 2107.07511
title: A Gentle Introduction to Conformal Prediction and Distribution-Free Uncertainty Quantification
authors: Anastasios N. Angelopoulos, Stephen Bates
venue: Foundations and Trends in Machine Learning, 2023 (arXiv 2021)
tier: foundations
threat_level: low
relevance_tags: [tutorial, split-CP, distribution-free, marginal-coverage, backbone]
---

## Abstract (verbatim)
Black-box machine learning models are now routinely used in high-risk settings, like medical diagnostics, which demand uncertainty quantification to avoid consequential model failures. Conformal prediction is a user-friendly paradigm for creating statistically rigorous uncertainty sets/intervals for the predictions of such models. Critically, the sets are valid in a distribution-free sense: they possess explicit, non-asymptotic guarantees even without distributional assumptions or model assumptions. One can use conformal prediction with any pre-trained model, such as a neural network, to produce sets that are guaranteed to contain the ground truth with a user-specified probability, such as 90%. It is easy-to-understand, easy-to-use, and general, applying naturally to problems arising in the fields of computer vision, natural language processing, deep reinforcement learning, and so on.

This hands-on introduction is aimed to provide the reader a working understanding of conformal prediction and related distribution-free uncertainty quantification techniques with one self-contained document. We lead the reader through practical theory for and examples of conformal prediction and describe its extensions to complex machine learning tasks involving structured outputs, distribution shift, time-series, outliers, models that abstain, and more. Throughout, there are many explanatory illustrations, examples, and code samples in Python. With each code sample comes a Jupyter notebook implementing the method on a real-data example; the notebooks can be accessed and easily run using our codebase.

## Key idea (3-5 sentences)
A self-contained, applied tutorial that frames split (inductive) conformal prediction as the canonical recipe for distribution-free uncertainty quantification on any pre-trained black-box model. Using only a held-out calibration set and an arbitrary nonconformity (score) function, one constructs a prediction set guaranteed to satisfy a finite-sample marginal coverage bound 1 - alpha <= P(Y_test in C(X_test)) <= 1 - alpha + 1/(n+1) under exchangeability of calibration and test data. The exposition then surveys extensions: adaptive sets (APS/RAPS) for conditional coverage, weighted CP for distribution shift, time-series CP, outliers, and abstention/cascades. The unifying message is that CP separates statistical guarantee (assumption-light) from heuristic score quality (model-dependent), so improving the score improves efficiency without breaking validity.

## Method / theorem statements
- **Split CP coverage bound** (the workhorse): given i.i.d. (or exchangeable) calibration set {(X_i, Y_i)}_{i=1}^n and test point (X_{n+1}, Y_{n+1}), let s(x, y) be a nonconformity score and let q_hat be the ceil((n+1)(1 - alpha))/n empirical quantile of {s(X_i, Y_i)}. Then C(X_{n+1}) = {y : s(X_{n+1}, y) <= q_hat} satisfies P(Y_{n+1} in C(X_{n+1})) >= 1 - alpha, with upper bound 1 - alpha + 1/(n+1) when scores are almost surely distinct.
- **Assumptions**: exchangeability of (X_i, Y_i)_{i=1..n+1}; no distributional or model assumptions otherwise.
- **Slack term**: O(1/n) finite-sample slack from the discrete quantile correction.
- **Score-design principle**: the choice of s controls *efficiency* (set size, conditional coverage) but not validity. Standard scores discussed: 1 - softmax_y for classification, residual / quantile-CQR for regression, APS cumulative-mass score for adaptive sets.
- **Listed extensions** (each derived in dedicated chapters): weighted CP under covariate shift; nexCP for non-exchangeable streams; risk-control variants (CRC, LTT); cascades / abstention; group-conditional and Mondrian CP.

## Reported results
- Tutorial; the document's "results" are pedagogical rather than empirical. It reports illustrative coverage / set-size diagnostics across ImageNet (RAPS), polyp segmentation (CRC-style), and medical-image classification examples (see paper for details).
- Reproduces the standard finite-sample 1 - alpha to 1 - alpha + 1/(n+1) coverage bound numerically across the worked examples.

## How CoT-CP uses this paper
- **Direct dependency**: We inherit the split-CP coverage statement and quantile-construction recipe verbatim. CoT-CP's chain-level conformity score is plugged into exactly this template (calibration -> empirical quantile -> threshold -> selective accept). Notation conventions (alpha, q_hat, n+1 correction) follow this paper.
- **Required citation**: yes - cited in Section 2 (Background: Split Conformal Prediction) and again in Section 5 (Discussion of distribution-free guarantees) of the CoT-CP paper.
- **Differentiator (none — foundational)**: N/A. We position CoT-CP as a *new score function* on top of the framework articulated here, not as a competitor.

## Citation key
angelopoulos2023gentle
